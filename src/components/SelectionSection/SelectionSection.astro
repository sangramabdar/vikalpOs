---
import Container from "../../layouts/Container.astro";
import SelectionSectionContent from "./SelectionSectionContent.tsx";

export interface Props {
  data: any;
}

const { data } = Astro.props as Props;
---

<Container headers={data.headers} description={data.description}>
  <SelectionSectionContent client:load data={data} />
</Container>
